
spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/test_db?currentSchema=public
    username: postgres
    password: password
  servlet:
    multipart:
      enabled: true 
      max-file-size: 2MB
      max-request-size: 5MB